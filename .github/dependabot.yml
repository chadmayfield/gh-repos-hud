version: 2
updates:
  # Go module dependencies. (GitHub Actions are intentionally SHA-pinned to
  # specific majors, so they are not watched here to avoid major-bump PRs.)
  - package-ecosystem: gomod
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    commit-message:
      prefix: "deps"
